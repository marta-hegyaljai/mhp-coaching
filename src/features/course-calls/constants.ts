/** Fixed public call length. Admin hours must align to this grid. */
export const CALL_DURATION_MINUTES = 15;

/** Do not offer a slot that starts sooner than this. */
export const CALL_MIN_NOTICE_MINUTES = 120;

/** Visitors can book this far ahead, measured in Zurich calendar days. */
export const CALL_MAX_ADVANCE_DAYS = 56;
